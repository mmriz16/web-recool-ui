export default function Button({ children, variant = "primary", ...props }: { children: React.ReactNode, variant?: "primary" | "secondary" }) {
  return <button className={`text-[var(--text-primary)] px-4 py-2 border border-[var(--border-color)] rounded-xl ${variant === "primary" ? "bg-[var(--bg-secondary)] text-[var(--text-primary)]" : "bg-[var(--bg-secondary)] text-[var(--text-primary)]"}`} {...props}>{children}</button>;
}